#include <stdio.h>
#include <stdlib.h>


int main()
{
    // d ou i   = inteiros em base decimais.
    // x        = hexadecimais.
    // f        = pontos flutuantes (com casas decimais).
    // e        = notação cientifica (com casas decimais).
    // c        = caracteres alfanumerico (textos).
    // s        = sequencia de caracteres alfanumerico (texto).
    // .<num>   = especificar quantos digitos serão impressos apos a virgula.

    
    // \a       = efeito sonoro do sistema.
    // \b       = backspace.
    // \n       = quebra de linha.
    // \t       = tabulação horizontal.
    // \r       = Retorna o inicio da linha.
    // \o       = caractere nulo.
    // \v       = Tabulação vertical.

    printf("Numero: %d %.2f %d \n", 10, 25.35, 36);

    system("pause");
    return 0;
}


